{
  "name": "emucorex-news-media-hub",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-hub.mjs",
    "validate": "node scripts/validate-hub.mjs --release",
    "validate:work": "node scripts/validate-hub.mjs",
    "keys:generate": "node scripts/generate-channel-keys.mjs",
    "channel:publish": "node scripts/publish-channel.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
